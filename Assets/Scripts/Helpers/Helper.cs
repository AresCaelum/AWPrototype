using UnityEngine;
using System.Collections;

public enum EnemyType { SLIME, BATTERY, };
public enum SlimeSize { BIGGEST, BIG, MEDIUM, SMALL}
public enum EntityType { ENEMY, BLOCK, SPAWNER, BATTERY}
//public enum PowerUps { HOOK, MULTI_SHOT, SPEED_BULLET, TIMER, BOMB, SHOOTING_PARTNERS, SHIELD}

public class Helper : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    
}
